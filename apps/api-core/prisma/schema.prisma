// Prisma schema — datasource + generator placeholder
// Tables will be added by individual feature specs.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
